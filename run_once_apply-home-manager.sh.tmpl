#!/usr/bin/env bash
# Run once: apply Home Manager configuration (Linux only)
set -e

{{- if eq .chezmoi.os "linux" }}
nix run home-manager/master -- switch --flake "{{ .chezmoi.sourceDir }}#$(hostname)"
{{- else }}
echo "Home Manager application skipped on non-Linux system"
{{- end }}
